image: markhobson/maven-chrome:jdk-8

stages:
  - build 
  - prepare 

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository/
    - target/

build:
  stage: build
  script:
    - mvn compile

prepare1:
  stage: prepare
  script: 
    - wipCommits=`git log -1 --pretty=%B`;module="invoice"; if [[ $wipCommits == $module ]]; then mvn test -Dtest=com.eliasnogueira.wdm.GithubWDMPageTest; fi	
    - wipCommits=`git log -1 --pretty=%B`;module="ippayments"; if [[ $wipCommits == $module ]]; then mvn test -Dtest=com.eliasnogueira.wdm.GithubWDMPageTest2; fi
    - echo "Successfully executed"
